<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.example.niklasjang.bottomnavigationbar_with_fragment_example">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".Activitys.SplashActivity">
            <intent-filter>

                <!-- Specify the ACTION_VIEW intent action so that the intent filter can be reached from Google Search. -->
                <action android:name="android.intent.action.VIEW"/>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".Activitys.MainActivity"
                android:label="@string/app_name">
        </activity>
        <activity android:name=".Activitys.RegisterActivity">
        </activity>
        <activity android:name=".Activitys.LoginActivity">
        </activity>
        <activity
                android:name=".Activitys.FilterActivity"
                android:windowSoftInputMode="adjustPan">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value=".Activitys.MainActivity"/>
        </activity>
        <activity android:name=".Activitys.MakePostActivity">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value=".Activitys.MainActivity"/>
        </activity>
        <activity android:name=".Activitys.PostLogActivity">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value=".Activitys.MainActivity"/>
        </activity>
        <activity android:name=".Activitys.PreferenceActivity">
        </activity>
    </application>

</manifest>